#include <stdio.h>
#include <iostream>
#include <cmath>
#include <vector>
#include "formula.hpp"

std::vector<std::vector<double>>weight_generator(int sim, int num){
  std::vector<std::vector<double>> weights;
  std::vector<double> weight;
  for(int i = 0; i < num; ++i){
    
  }
}